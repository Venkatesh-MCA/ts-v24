import { IdinarupeesPipe } from './idinarupees.pipe';

describe('IdinarupeesPipe', () => {
  it('create an instance', () => {
    const pipe = new IdinarupeesPipe();
    expect(pipe).toBeTruthy();
  });
});
