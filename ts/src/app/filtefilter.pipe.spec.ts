import { FiltefilterPipe } from './filtefilter.pipe';

describe('FiltefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
